import { ReactNode } from "react";
export declare function TooltipContextProvider({ children }: {
    children: ReactNode;
}): JSX.Element;
export declare function useTooltip(): {
    showTooltip: (x: number, y: number, text: string) => void;
    hideTooltip: () => void;
    isTooltipVisible: boolean;
    x: number;
    y: number;
    text: string;
};
